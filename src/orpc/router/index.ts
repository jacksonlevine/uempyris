import { addTodo, listTodos } from './todos'
import { addBrand, addProduct, listBrands, listProducts } from './catalog'

export default {
  listTodos,
  addTodo,
  listBrands,
  listProducts,
  addBrand,
  addProduct,
}
